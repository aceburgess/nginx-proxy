version: '2'
services:
  nginx:
    restart: always
    image: nginx
    container_name: nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/nginx/conf.d
      - ./vhost.d:/etc/nginx/vhost.d:rw
      - /usr/share/nginx/html
      - /usr/src/app/static:/usr/src/app/static:ro
      - /usr/src/nginx-proxy-ssl/certs:/etc/nginx/certs:ro

  dockergen:
    restart: always
    image: jwilder/docker-gen
    container_name: docker-gen
    command: -notify-sighup nginx -watch /etc/docker-gen/templates/nginx.tmpl /etc/nginx/conf.d/default.conf
    volumes_from:
      - nginx
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - ./nginx.tmpl:/etc/docker-gen/templates/nginx.tmpl

  letsencrypt:
    restart: always
    image: jrcs/letsencrypt-nginx-proxy-companion
    container_name: lets-encrypt
    volumes_from:
      - nginx
    volumes:
      - /usr/src/nginx-proxy-ssl/certs:/etc/nginx/certs:rw
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - NGINX_DOCKER_GEN_CONTAINER=docker-gen
    
  jenkins:
    restart: always
    image: jenkinsci/jenkins
    container_name: jenkins
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - /usr/src/nginx-proxy-ssl/jenkins-data:/var/jenkins_home:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - /usr/src/app:/usr/src/app:rw
    #environment:
      #- VIRTUAL_HOST=jenkins.seenonvhs.com
      #- LETSENCRYPT_HOST=jenkins.seenonvhs.com
      #- LETSENCRYPT_TEST=true

networks:
  default:
    external:
      name: proxy
